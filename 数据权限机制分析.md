# Ruoyi-Vue-Pro 数据权限机制深度分析

## 一、数据权限转换流程：从角色配置到 SQL 查询条件

### 1.1 角色配置存储

角色的数据权限配置存储在 `system_role` 表中，核心字段包括：

- **data_scope**：数据范围类型（枚举值）
- **data_scope_dept_ids**：自定义部门数组（仅当 data_scope=2 时生效）

```java
// RoleDO.java (yudao-module-system/src/main/java/cn/iocoder/yudao/module/system/dal/dataobject/permission/RoleDO.java:64-76)
/**
 * 数据范围
 *
 * 枚举 {@link DataScopeEnum}
 */
private Integer dataScope;
/**
 * 数据范围(指定部门数组)
 *
 * 适用于 {@link #dataScope} 的值为 {@link DataScopeEnum#DEPT_CUSTOM} 时
 */
@TableField(typeHandler = JacksonTypeHandler.class)
private Set<Long> dataScopeDeptIds;
```

### 1.2 数据权限计算逻辑

当用户发起查询时，系统通过 `PermissionServiceImpl.getDeptDataPermission()` 方法计算用户的实际数据权限范围：

```java
// PermissionServiceImpl.java (yudao-module-system/src/main/java/cn/iocoder/yudao/module/system/service/permission/PermissionServiceImpl.java:276-335)
@DataPermission(enable = false) // 关闭数据权限，避免递归
public DeptDataPermissionRespDTO getDeptDataPermission(Long userId) {
    // 1. 获取用户所有启用状态的角色
    List<RoleDO> roles = getEnableUserRoleListByUserIdFromCache(userId);
    
    DeptDataPermissionRespDTO result = new DeptDataPermissionRespDTO();
    for (RoleDO role : roles) {
        // 2. 根据角色的 dataScope 计算权限
        // 情况一：ALL(1) - 全部数据权限
        if (Objects.equals(role.getDataScope(), DataScopeEnum.ALL.getScope())) {
            result.setAll(true);
            continue;
        }
        // 情况二：DEPT_CUSTOM(2) - 指定部门
        if (Objects.equals(role.getDataScope(), DataScopeEnum.DEPT_CUSTOM.getScope())) {
            CollUtil.addAll(result.getDeptIds(), role.getDataScopeDeptIds());
            CollectionUtils.addIfNotNull(result.getDeptIds(), userDeptId.get());
            continue;
        }
        // 情况三：DEPT_ONLY(3) - 本部门
        if (Objects.equals(role.getDataScope(), DataScopeEnum.DEPT_ONLY.getScope())) {
            CollectionUtils.addIfNotNull(result.getDeptIds(), userDeptId.get());
            continue;
        }
        // 情况四：DEPT_AND_CHILD(4) - 本部门及以下
        if (Objects.equals(role.getDataScope(), DataScopeEnum.DEPT_AND_CHILD.getScope())) {
            CollUtil.addAll(result.getDeptIds(), deptService.getChildDeptIdListFromCache(deptId));
            result.getDeptIds().add(deptId);
            continue;
        }
        // 情况五：SELF(5) - 仅本人
        if (Objects.equals(role.getDataScope(), DataScopeEnum.SELF.getScope())) {
            result.setSelf(true);
            continue;
        }
    }
    return result;
}
```

### 1.3 SQL 条件拼接流程

数据权限通过 MyBatis Plus 的 `MultiDataPermissionHandler` 拦截器实现 SQL 自动拼接：

```
用户查询请求
    ↓
MyBatis Plus DataPermissionInterceptor 拦截器
    ↓
DataPermissionRuleHandler.getSqlSegment() 处理
    ↓
DeptDataPermissionRule.getExpression() 生成条件表达式
    ↓
根据 deptIds 和 self 标志拼接 WHERE 条件
    ↓
执行最终 SQL
```

关键代码：

```java
// DataPermissionRuleHandler.java (yudao-framework/yudao-spring-boot-starter-biz-data-permission/src/main/java/cn/iocoder/yudao/framework/datapermission/core/db/DataPermissionRuleHandler.java:31-62)
public Expression getSqlSegment(Table table, Expression where, String mappedStatementId) {
    // 1. 获取该 Mapper 对应的数据权限规则
    List<DataPermissionRule> rules = ruleFactory.getDataPermissionRule(mappedStatementId);
    
    // 2. 遍历规则，为每个匹配的表生成条件
    Expression allExpression = null;
    for (DataPermissionRule rule : rules) {
        String tableName = MyBatisUtils.getTableName(table);
        if (!rule.getTableNames().contains(tableName)) {
            continue;
        }
        // 3. 调用规则生成条件表达式
        Expression oneExpress = rule.getExpression(tableName, table.getAlias());
        // 4. 多个规则用 AND 连接
        allExpression = allExpression == null ? oneExpress
                : new AndExpression(allExpression, oneExpress);
    }
    return allExpression;
}
```

## 二、五种数据权限范围的差异

### 2.1 数据范围枚举定义

```java
// DataScopeEnum.java (yudao-module-system/src/main/java/cn/iocoder/yudao/module/system/enums/permission/DataScopeEnum.java:18-38)
public enum DataScopeEnum implements ArrayValuable<Integer> {
    ALL(1),              // 全部数据权限
    DEPT_CUSTOM(2),      // 指定部门数据权限
    DEPT_ONLY(3),        // 部门数据权限（仅本部门）
    DEPT_AND_CHILD(4),   // 部门及以下数据权限
    SELF(5);             // 仅本人数据权限
}
```

### 2.2 各范围详细对比

| 范围类型 | 枚举值 | 生成的 SQL 条件 | 适用场景 |
|---------|-------|----------------|---------|
| **全部数据** | 1 | 不拼接任何条件（无条件） | 超级管理员、系统级查询 |
| **指定部门** | 2 | `dept_id IN (自定义部门ID列表)` | 需要跨部门查看特定部门数据的角色 |
| **本部门** | 3 | `dept_id = 当前用户部门ID` | 只能查看自己部门数据的角色 |
| **本部门及以下** | 4 | `dept_id IN (当前部门 + 所有子部门ID)` | 部门负责人、层级管理角色 |
| **仅本人** | 5 | `user_id = 当前用户ID` | 只能查看自己创建的数据 |

### 2.3 多角色权限合并规则

当用户拥有多个角色时，系统采用 **"取并集"** 的策略：

- 如果任何一个角色的 `dataScope = ALL`，则最终权限为 `all = true`
- 所有角色的 `deptIds` 合并为一个 Set（去重）
- 如果任何一个角色的 `dataScope = SELF`，则 `self = true`

最终查询条件：`(dept_id IN (合并后的deptIds)) OR (user_id = 当前用户ID)`

## 三、表别名与字段识别机制

### 3.1 表配置注册

数据权限需要预先配置哪些表需要过滤，以及过滤的字段名：

```java
// DataPermissionConfiguration.java (yudao-module-system/src/main/java/cn/iocoder/yudao/module/system/framework/datapermission/config/DataPermissionConfiguration.java:17-26)
@Bean
public DeptDataPermissionRuleCustomizer sysDeptDataPermissionRuleCustomizer() {
    return rule -> {
        // 配置基于 dept_id 过滤的表
        rule.addDeptColumn(AdminUserDO.class);  // system_users 表，字段 dept_id
        rule.addDeptColumn(DeptDO.class, "id");  // system_dept 表，字段 id
        
        // 配置基于 user_id 过滤的表
        rule.addUserColumn(AdminUserDO.class, "id");  // system_users 表，字段 id
    };
}
```

### 3.2 表别名处理

数据权限通过 JSqlParser 解析 SQL 中的表结构，自动识别表别名：

```java
// DeptDataPermissionRule.java (yudao-framework/yudao-spring-boot-starter-biz-data-permission/src/main/java/cn/iocoder/yudao/framework/datapermission/core/rule/dept/DeptDataPermissionRule.java:148-175)
private Expression buildDeptExpression(String tableName, Alias tableAlias, Set<Long> deptIds) {
    // 1. 从配置中获取该表的部门字段名
    String columnName = deptColumns.get(tableName);
    if (StrUtil.isEmpty(columnName)) {
        return null;
    }
    
    // 2. 构建带别名的列名（如：u.dept_id）
    return new InExpression(
        MyBatisUtils.buildColumn(tableName, tableAlias, columnName),
        new ParenthesedExpressionList(new ExpressionList<LongValue>(...))
    );
}
```

### 3.3 字段映射配置

- **deptColumns Map**：`key = 表名`，`value = 部门字段名`（默认为 `dept_id`）
- **userColumns Map**：`key = 表名`，`value = 用户字段名`（默认为 `user_id`）

系统通过 `MyBatisUtils.buildColumn()` 自动处理表别名：
- 无别名：`table_name.column_name`
- 有别名：`alias.column_name`

## 四、绕过数据权限的方法和场景

### 4.1 通过注解绕过

使用 `@DataPermission(enable = false)` 注解可以禁用数据权限：

```java
// PermissionServiceImpl.java (yudao-module-system/src/main/java/cn/iocoder/yudao/module/system/service/permission/PermissionServiceImpl.java:276-277)
@DataPermission(enable = false) // 关闭数据权限，避免递归获取数据权限
public DeptDataPermissionRespDTO getDeptDataPermission(Long userId) {
    // 此方法内的所有查询都不会应用数据权限
}
```

### 4.2 通过编程式绕过

使用 `DataPermissionUtils` 手动控制：

```java
// DataPermissionUtils.java (yudao-framework/yudao-spring-boot-starter-biz-data-permission/src/main/java/cn/iocoder/yudao/framework/datapermission/core/util/DataPermissionUtils.java:61-70)
public static void addDisableDataPermission(){
    DataPermission dataPermission = getDisableDataPermissionDisable();
    DataPermissionContextHolder.add(dataPermission);
}

public static void removeDataPermission(){
    DataPermissionContextHolder.remove();
}
```

使用方式：
```java
try {
    DataPermissionUtils.addDisableDataPermission();
    // 这里的查询会绕过数据权限
    userMapper.selectById(id);
} finally {
    DataPermissionUtils.removeDataPermission();
}
```

### 4.3 特殊场景自动绕过

1. **数据翻译场景**：框架自动检测并跳过
```java
// DataPermissionRuleFactoryImpl.java 中的特殊处理
if (isTranslateCall()) {
    return Collections.emptyList(); // 数据翻译时强制忽略数据权限
}
```

2. **非管理员用户**：数据权限仅对管理员类型用户生效
```java
// DeptDataPermissionRule.java:98-100
if (ObjectUtil.notEqual(loginUser.getUserType(), UserTypeEnum.ADMIN.getValue())) {
    return null; // 非管理员不处理数据权限
}
```

3. **跨租户访问**：通过 `skipPermissionCheck()` 检测
```java
if (skipPermissionCheck()) {
    return null;
}
```

### 4.4 无配置的表自然绕过

如果业务表没有在 `DeptDataPermissionRuleCustomizer` 中配置，数据权限规则不会匹配该表，也就不会添加任何过滤条件。

## 五、各种查询场景的误判风险分析

### 5.1 分页查询

**风险：** 数据权限在分页插件之前执行，条件拼接正确，但需要注意：

```
原始 SQL: SELECT * FROM business WHERE status = 1 LIMIT 10 OFFSET 0
拼接后:  SELECT * FROM business WHERE status = 1 AND dept_id IN (1,2) LIMIT 10 OFFSET 0
```

**潜在问题：**
- 分页总数计算也会被数据权限过滤，这是预期行为
- 但如果在 Service 层自己统计总数，需要确保同样应用了数据权限

### 5.2 联表查询（LEFT JOIN / INNER JOIN / RIGHT JOIN）

**风险等级：高**

数据权限对 JOIN 的处理方式：

```java
// 测试用例展示 (DataPermissionRuleHandlerTest.java:202-248)

// LEFT JOIN - 条件加在 ON 子句（驱动表条件加在 WHERE）
SELECT * FROM entity e 
LEFT JOIN entity1 e1 ON e1.id = e.id 
WHERE e.id = ?

// 拼接后：
SELECT * FROM entity e 
LEFT JOIN entity1 e1 ON e1.id = e.id AND e1.dept_id IN (10, 20)
WHERE e.id = ? AND e.dept_id IN (10, 20)

// INNER JOIN - 两个表的条件都加在 ON 子句
SELECT * FROM entity e 
INNER JOIN entity1 e1 ON e1.id = e.id AND e.dept_id IN (10, 20) AND e1.dept_id IN (10, 20)
WHERE e.id = ?

// RIGHT JOIN - 右侧表条件加在 WHERE
SELECT * FROM entity e 
RIGHT JOIN entity1 e1 ON e1.id = e.id AND e.dept_id IN (10, 20)
WHERE e.id = ? AND e1.dept_id IN (10, 20)
```

**误判场景：**

```sql
-- 场景1：查询业务数据关联用户信息
SELECT b.*, u.username 
FROM business b 
LEFT JOIN system_users u ON b.user_id = u.id
WHERE b.status = 1

-- 数据权限会给两个表都加条件：
-- b.dept_id IN (...) AND u.dept_id IN (...)
-- 问题：如果用户表的 dept_id 和业务表的 dept_id 语义不同，会导致误过滤
```

**解决方案：**
- 确保所有关联表的 dept_id/user_id 语义一致
- 或者使用 `@DataPermission` 精确控制哪些规则生效

### 5.3 子查询

**风险等级：高**

数据权限会递归处理子查询中的表：

```sql
-- 原始 SQL
SELECT * FROM business 
WHERE user_id IN (
    SELECT id FROM system_users WHERE status = 1
)

-- 拼接后（两个表都会被过滤）
SELECT * FROM business 
WHERE user_id IN (
    SELECT id FROM system_users WHERE status = 1 AND dept_id IN (1, 2)
) AND dept_id IN (1, 2)
```

**误判场景：**

```sql
-- 场景：查询所有部门负责人的业务数据
SELECT * FROM business 
WHERE user_id IN (
    SELECT leader_user_id FROM system_dept WHERE status = 1
)

-- 问题：
-- system_dept 表配置的是 id 作为部门字段（rule.addDeptColumn(DeptDO.class, "id")）
-- 所以会添加 id IN (1, 2) 条件
-- 这会错误地过滤掉其他部门的负责人数据
```

### 5.4 聚合统计（COUNT / SUM / AVG / GROUP BY）

**风险等级：中**

```sql
-- 原始 SQL
SELECT dept_id, COUNT(*) as cnt 
FROM business 
GROUP BY dept_id

-- 拼接后
SELECT dept_id, COUNT(*) as cnt 
FROM business 
WHERE dept_id IN (1, 2)  -- 只统计有权限的部门
GROUP BY dept_id
```

**潜在问题：**
- 统计结果只包含有权限的数据，这通常是正确的
- 但如果需要统计全局数据做对比，需要绕过数据权限

```sql
-- 问题场景：计算各部门占比
SELECT 
    dept_id,
    COUNT(*) as dept_count,
    COUNT(*) * 100.0 / (SELECT COUNT(*) FROM business) as percentage
FROM business 
GROUP BY dept_id

-- 拼接后，子查询也会被过滤，导致百分比永远是 100%
```

### 5.5 隐式连接（逗号分隔的多表查询）

**风险等级：中**

```sql
-- 隐式内连接
SELECT * FROM entity, entity1 WHERE entity.id = entity1.id

-- 拼接后
SELECT * FROM entity, entity1 
WHERE entity.id = entity1.id 
  AND entity.dept_id IN (1, 2) 
  AND entity1.dept_id IN (1, 2)
```

**问题：** 所有表都会被添加同样的权限条件，可能导致逻辑错误。

## 六、三表联查示例：用户表 + 部门表 + 业务表

### 6.1 表结构设计

```sql
-- 1. 用户表 (system_users)
CREATE TABLE system_users (
    id BIGINT PRIMARY KEY,
    username VARCHAR(50),
    nickname VARCHAR(50),
    dept_id BIGINT,           -- 部门ID（数据权限字段）
    status INT,
    create_time DATETIME
);

-- 2. 部门表 (system_dept)
CREATE TABLE system_dept (
    id BIGINT PRIMARY KEY,     -- 部门ID（数据权限字段，用 id 作为过滤字段）
    name VARCHAR(100),
    parent_id BIGINT,
    leader_user_id BIGINT,     -- 负责人ID
    status INT
);

-- 3. 业务表 (假设为 crm_customer)
CREATE TABLE crm_customer (
    id BIGINT PRIMARY KEY,
    name VARCHAR(100),
    mobile VARCHAR(20),
    dept_id BIGINT,           -- 归属部门（数据权限字段）
    owner_user_id BIGINT,     -- 负责人ID
    status INT,
    create_time DATETIME
);
```

### 6.2 数据权限配置

```java
// 假设 CRM 模块的配置
@Configuration
public class CrmDataPermissionConfiguration {
    @Bean
    public DeptDataPermissionRuleCustomizer crmDataPermissionRuleCustomizer() {
        return rule -> {
            // 业务表通过 dept_id 过滤
            rule.addDeptColumn(CrmCustomerDO.class);
            // 业务表通过 owner_user_id 过滤（自定义字段名）
            rule.addUserColumn(CrmCustomerDO.class, "owner_user_id");
        };
    }
}
```

### 6.3 三表联查示例

**场景：** 查询客户列表，关联用户信息和部门名称

```java
// Mapper 查询（伪代码）
@Select("SELECT c.*, u.nickname as owner_name, d.name as dept_name " +
        "FROM crm_customer c " +
        "LEFT JOIN system_users u ON c.owner_user_id = u.id " +
        "LEFT JOIN system_dept d ON c.dept_id = d.id " +
        "WHERE c.status = 1")
List<CrmCustomerVO> selectCustomerList();
```

**原始 SQL：**
```sql
SELECT c.*, u.nickname as owner_name, d.name as dept_name 
FROM crm_customer c 
LEFT JOIN system_users u ON c.owner_user_id = u.id 
LEFT JOIN system_dept d ON c.dept_id = d.id 
WHERE c.status = 1
```

**数据权限拼接后的 SQL（假设用户权限：deptIds=[1,2], self=true, userId=100）：**

```sql
SELECT c.*, u.nickname as owner_name, d.name as dept_name 
FROM crm_customer c 
LEFT JOIN system_users u 
    ON c.owner_user_id = u.id 
    AND u.dept_id IN (1, 2)                    -- 用户表的权限条件
LEFT JOIN system_dept d 
    ON c.dept_id = d.id 
    AND d.id IN (1, 2)                         -- 部门表的权限条件
WHERE c.status = 1 
  AND (c.dept_id IN (1, 2) OR c.owner_user_id = 100)  -- 业务表的权限条件
```

### 6.4 潜在问题分析

**问题1：** 三个表都被添加了权限条件，可能导致过滤过度

- 如果客户归属部门 A，但负责人属于部门 B，且用户只能查看部门 A
- `u.dept_id IN (1, 2)` 可能过滤掉负责人信息
- 导致查询结果中 owner_name 为 NULL

**问题2：** 部门表的权限条件语义不同

- 部门表配置的是 `id` 作为过滤字段
- 条件 `d.id IN (1, 2)` 意味着只能查看部门 1、2 的名称
- 这可能不是预期行为（应该能看到所有部门名称，只是数据受限制）

**问题3：** 复杂的 OR 条件组合

- 业务表的条件是 `(dept_id IN ?) OR (owner_user_id = ?)`
- 如果还有其他业务条件，需要特别注意括号位置

## 七、只看 Controller 会漏掉的关键信息

### 7.1 数据权限配置层

**Controller 层面看不到：**

```java
// 这些配置在 Configuration 中，Controller 完全无感知
@Configuration
public class DataPermissionConfiguration {
    @Bean
    public DeptDataPermissionRuleCustomizer sysDeptDataPermissionRuleCustomizer() {
        return rule -> {
            rule.addDeptColumn(AdminUserDO.class);
            rule.addDeptColumn(DeptDO.class, "id");
            rule.addUserColumn(AdminUserDO.class, "id");
        };
    }
}
```

**影响：**
- 不知道哪些表会被数据权限过滤
- 不知道每个表用什么字段过滤
- 无法预测联表查询的行为

### 7.2 Mapper 实现层

**Controller 只能看到接口定义：**

```java
// Controller 看到的
@GetMapping("/page")
public CommonResult<PageResult<UserRespVO>> getUserPage(UserPageReqVO reqVO) {
    PageResult<AdminUserDO> pageResult = userService.getUserPage(reqVO);
    return success(BeanUtils.toBean(pageResult, UserRespVO.class));
}

// 但实际的查询在 Mapper 中
default PageResult<AdminUserDO> selectPage(UserPageReqVO reqVO, 
                                            Collection<Long> deptIds, 
                                            Collection<Long> userIds) {
    return selectPage(reqVO, new LambdaQueryWrapperX<AdminUserDO>()
        .likeIfPresent(AdminUserDO::getUsername, reqVO.getUsername())
        .inIfPresent(AdminUserDO::getDeptId, deptIds)  // 手动过滤
        .orderByDesc(AdminUserDO::getId));
}
```

**漏掉的信息：**
- Mapper 是否有手动的 dept_id 过滤
- 是否使用了原生 SQL（@Select）
- 是否存在联表查询、子查询

### 7.3 Service 层的注解控制

**关键注解可能在 Service 层：**

```java
// Controller 看不到这个注解
@Service
public class PermissionServiceImpl {
    
    @DataPermission(enable = false)  // 绕过数据权限
    public DeptDataPermissionRespDTO getDeptDataPermission(Long userId) {
        // ...
    }
    
    @DataPermission(includeRules = {TenantDataPermissionRule.class})  // 只启用部分规则
    public void someMethod() {
        // ...
    }
}
```

**影响：**
- 无法知道哪些查询绕过了数据权限
- 无法知道使用了哪些权限规则

### 7.4 全局拦截器行为

**数据权限是全局生效的：**

```java
// 自动配置类，全局生效
@AutoConfiguration
public class YudaoDataPermissionAutoConfiguration {
    
    @Bean
    public DataPermissionRuleHandler dataPermissionRuleHandler(...) {
        DataPermissionInterceptor inner = new DataPermissionInterceptor(handler);
        MyBatisUtils.addInterceptor(interceptor, inner, 0);  // 添加为首个拦截器
        return handler;
    }
}
```

**漏掉的信息：**
- 数据权限拦截器的优先级
- 是否在分页插件之前执行
- 与其他拦截器的交互

### 7.5 表字段语义差异

**相同的字段名，不同的语义：**

```java
// AdminUserDO - dept_id 表示用户所属部门
private Long deptId;

// DeptDO - id 就是部门ID
private Long id;

// CrmCustomerDO - dept_id 表示数据归属部门
private Long deptId;
```

**只看 Controller 无法知道：**
- `dept_id` 在不同表中的具体含义
- 这些表如何关联
- 权限过滤是否会导致逻辑错误

### 7.6 权限计算逻辑

**角色权限合并逻辑是复杂的：**

```java
// 只看 Controller 不知道：
// - 多角色权限如何合并
// - 自定义部门是否包含当前部门
// - SELF 和 DEPT 条件如何组合
// - 缓存机制如何工作
```

**实际逻辑：**
```java
for (RoleDO role : roles) {
    if (ALL) result.setAll(true);
    if (DEPT_CUSTOM) {
        CollUtil.addAll(result.getDeptIds(), role.getDataScopeDeptIds());
        CollectionUtils.addIfNotNull(result.getDeptIds(), userDeptId.get()); // 自动包含当前部门
    }
    // ...
}
```

### 7.7 总结：完整分析需要的视角

| 层级 | 必须查看的文件 | 能获得的信息 |
|-----|---------------|-------------|
| **Controller** | Controller 类 | 接口定义、参数校验、权限注解（@PreAuthorize） |
| **Service** | Service 实现类 | @DataPermission 注解、业务逻辑、手动权限控制 |
| **Mapper** | Mapper 接口和 XML | 实际 SQL、联表查询、手动过滤条件 |
| **Configuration** | 自动配置类 | 拦截器注册顺序、全局配置 |
| **Rule** | DeptDataPermissionRule 等 | 表配置、字段映射、条件生成逻辑 |
| **Enum/DO** | DataScopeEnum、RoleDO | 数据范围定义、存储结构 |

**结论：** 只看 Controller 只能看到"入口"，无法理解完整的数据权限机制。必须深入到 Service、Mapper、Configuration 甚至拦截器层，才能全面掌握数据权限的实际行为。

## 八、关键代码文件位置汇总

| 文件 | 功能 | 路径 |
|-----|------|-----|
| DataPermission.java | 数据权限注解 | framework/.../datapermission/core/annotation/ |
| DataPermissionAnnotationInterceptor.java | AOP 切面拦截 | framework/.../datapermission/core/aop/ |
| DataPermissionRuleHandler.java | SQL 条件处理器 | framework/.../datapermission/core/db/ |
| DeptDataPermissionRule.java | 部门权限规则实现 | framework/.../datapermission/core/rule/dept/ |
| DataPermissionConfiguration.java | system 模块表配置 | module-system/.../framework/datapermission/config/ |
| PermissionServiceImpl.java | 权限计算逻辑 | module-system/.../service/permission/ |
| DataScopeEnum.java | 数据范围枚举 | module-system/.../enums/permission/ |
| RoleDO.java | 角色数据模型 | module-system/.../dal/dataobject/permission/ |
| AdminUserDO.java | 用户数据模型 | module-system/.../dal/dataobject/user/ |
| DeptDO.java | 部门数据模型 | module-system/.../dal/dataobject/dept/ |
