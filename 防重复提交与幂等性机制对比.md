# ruoyi-vue-pro 防重复提交与幂等性机制对比分析

## 一、四种机制概述

在 ruoyi-vue-pro 项目中，存在四层防重复提交和幂等控制机制，它们各司其职，从前端到数据库形成完整的防护体系。

### 1. 前端防重复提交

**实现位置**：前端 Vue 组件，通过按钮 `loading` 状态控制

**核心实现**：

```vue
<template>
  <el-button type="primary" @click="submitForm" :loading="formLoading">确定</el-button>
</template>

<script setup lang="ts">
const formLoading = ref(false)

const submitForm = async () => {
  await formRef.value.validate()
  formLoading.value = true  // 请求前设置 loading
  try {
    await WmSnApi.generateSnCodes(formData.value)
    message.success('生成成功')
  } finally {
    formLoading.value = false  // 无论成功失败都重置
  }
}
</script>
```

**代码位置**：`yudao-ui/yudao-ui-admin-vue3/src/views/mes/wm/sn/index.vue:210-221`

---

### 2. 后端幂等控制

**实现位置**：`@Idempotent` 注解 + AOP 切面

**核心实现**：

**注解定义**：`yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/idempotent/core/annotation/Idempotent.java:1-62`

```java
@Target({ElementType.METHOD})
@Retention(RetentionPolicy.RUNTIME)
public @interface Idempotent {
    int timeout() default 1;  // 幂等超时时间，默认 1 秒
    String message() default "重复请求，请稍后重试";
    Class<? extends IdempotentKeyResolver> keyResolver() default DefaultIdempotentKeyResolver.class;
    boolean deleteKeyWhenException() default true;  // 异常时删除 Key，允许重试
}
```

**AOP 切面**：`yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/idempotent/core/aop/IdempotentAspect.java:1-66`

```java
@Aspect
public class IdempotentAspect {
    
    @Around(value = "@annotation(idempotent)")
    public Object aroundPointCut(ProceedingJoinPoint joinPoint, Idempotent idempotent) throws Throwable {
        // 1. 解析幂等 Key（方法名 + 参数，MD5 压缩）
        IdempotentKeyResolver keyResolver = keyResolvers.get(idempotent.keyResolver());
        String key = keyResolver.resolver(joinPoint, idempotent);
        
        // 2. Redis SETNX 原子操作锁定
        boolean success = idempotentRedisDAO.setIfAbsent(key, idempotent.timeout(), idempotent.timeUnit());
        if (!success) {
            throw new ServiceException(GlobalErrorCodeConstants.REPEATED_REQUESTS.getCode(), idempotent.message());
        }
        
        // 3. 执行业务逻辑
        try {
            return joinPoint.proceed();
        } catch (Throwable throwable) {
            // 4. 异常时删除 Key，允许下次重试（参考美团 GTIS 思路）
            if (idempotent.deleteKeyWhenException()) {
                idempotentRedisDAO.delete(key);
            }
            throw throwable;
        }
    }
}
```

**Redis DAO**：`yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/idempotent/core/redis/IdempotentRedisDAO.java:1-41`

**Key 生成器**：`yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/idempotent/core/keyresolver/impl/DefaultIdempotentKeyResolver.java:1-24`

```java
public class DefaultIdempotentKeyResolver implements IdempotentKeyResolver {
    @Override
    public String resolver(JoinPoint joinPoint, Idempotent idempotent) {
        String methodName = joinPoint.getSignature().toString();
        String argsStr = StrUtils.joinMethodArgs(joinPoint);
        return SecureUtil.md5(methodName + argsStr);  // MD5 压缩避免 Key 过长
    }
}
```

---

### 3. 分布式锁

**实现位置**：Lock4j 框架 + Redisson 可重入锁

**核心实现**：

**自定义锁 DAO（钱包场景）**：`yudao-module-pay/src/main/java/cn/iocoder/yudao/module/pay/dal/redis/wallet/PayWalletLockRedisDAO.java:1-42`

```java
@Repository
public class PayWalletLockRedisDAO {
    
    @Resource
    private RedissonClient redissonClient;
    
    public <V> V lock(Long id, Long timeoutMillis, Callable<V> callable) throws Exception {
        String lockKey = formatKey(id);
        RLock lock = redissonClient.getLock(lockKey);
        try {
            lock.lock(timeoutMillis, TimeUnit.MILLISECONDS);
            return callable.call();  // 执行业务逻辑
        } finally {
            lock.unlock();  // 确保释放锁
        }
    }
}
```

**实际使用示例（钱包创建）**：`yudao-module-pay/src/main/java/cn/iocoder/yudao/module/pay/service/wallet/PayWalletServiceImpl.java:62-78`

```java
public PayWalletDO getOrCreateWallet(Long userId, Integer userType) {
    PayWalletDO wallet = walletMapper.selectByUserIdAndType(userId, userType);
    if (wallet == null) {
        // 双重检查锁 + 分布式锁
        wallet = lockRedisDAO.lock(userId, UPDATE_TIMEOUT_MILLIS, () -> {
            PayWalletDO newWallet = walletMapper.selectByUserIdAndType(userId, userType);
            if (newWallet == null) {
                newWallet = new PayWalletDO().setUserId(userId).setUserType(userType)
                        .setBalance(0).setTotalExpense(0).setTotalRecharge(0);
                newWallet.setCreateTime(LocalDateTime.now());
                walletMapper.insert(newWallet);
            }
            return newWallet;
        });
    }
    return wallet;
}
```

**钱包余额扣减（带锁）**：`yudao-module-pay/src/main/java/cn/iocoder/yudao/module/pay/service/wallet/PayWalletServiceImpl.java:145-187`

```java
public PayWalletTransactionDO reduceWalletBalance(Long walletId, Long bizId, 
                                                  PayWalletBizTypeEnum bizType, Integer price) {
    return lockRedisDAO.lock(walletId, UPDATE_TIMEOUT_MILLIS, () -> {
        // 1. 基于乐观锁的余额扣减（SQL 条件判断）
        int updateCounts = walletMapper.updateWhenConsumption(payWallet.getId(), price);
        if (updateCounts == 0) {
            throw exception(WALLET_BALANCE_NOT_ENOUGH);
        }
        
        // 2. 生成钱包流水
        WalletTransactionCreateReqBO bo = new WalletTransactionCreateReqBO()
                .setWalletId(payWallet.getId()).setPrice(-price)
                .setBalance(afterBalance).setBizId(String.valueOf(bizId))
                .setBizType(bizType.getType()).setTitle(bizType.getDescription());
        return walletTransactionService.createWalletTransaction(bo);
    });
}
```

---

### 4. 数据库唯一索引

**实现位置**：数据库层面的 `UNIQUE KEY` 约束

**设计思路**：在 ruoyi-vue-pro 中，虽然 SQL 脚本未显式创建业务唯一索引，但在钱包充值和支付回调场景中，通过以下方式实现数据库层面的幂等：

1. **商户订单号唯一性**：`PayOrderDO.merchantOrderId` 字段应在应用层保证唯一性
2. **状态机防重**：支付回调时校验订单状态，只有未支付状态才执行扣款

**伪代码示例**：

```sql
-- 推荐添加的唯一索引设计
ALTER TABLE pay_order ADD UNIQUE KEY uk_app_merchant_order (app_id, merchant_order_id);
ALTER TABLE pay_wallet ADD UNIQUE KEY uk_user_type (user_id, user_type);
ALTER TABLE pay_wallet_transaction ADD UNIQUE KEY uk_wallet_biz (wallet_id, biz_id, biz_type);
```

**代码中状态机防重**：`yudao-module-pay/src/main/java/cn/iocoder/yudao/module/pay/service/wallet/PayWalletRechargeServiceImpl.java:119-158`

```java
public void updateWalletRechargerPaid(Long id, Long payOrderId) {
    // 1. 校验是否已支付（状态机防重）
    PayWalletRechargeDO recharge = walletRechargeMapper.selectById(id);
    if (recharge.getPayStatus()) {
        if (ObjectUtil.equals(recharge.getPayOrderId(), payOrderId)) {
            // 特殊：支付单号相同，直接返回，说明重复回调
            return;
        }
        throw exception(WALLET_RECHARGE_UPDATE_PAID_PAY_ORDER_ID_ERROR);
    }
    
    // 2. 乐观锁更新（CAS 思想）
    int updateCount = walletRechargeMapper.updateByIdAndPaid(id, false,
            new PayWalletRechargeDO().setId(id).setPayStatus(true)...);
    if (updateCount == 0) {
        throw exception(WALLET_RECHARGE_UPDATE_PAID_STATUS_NOT_UNPAID);
    }
}
```

---

## 二、职责边界对比

| 机制 | 层级 | 保护目标 | 典型场景 | 依赖组件 | 失效风险 |
|------|------|----------|----------|----------|----------|
| 前端 loading | 展示层 | 用户快速点击 | 表单提交、按钮点击 | Vue 响应式状态 | JS 可绕过、网络层不可控 |
| 后端幂等控制 | 业务层 | 短时间内重复请求 | HTTP 重复提交、重试 | Redis SETNX | Key 过期、异常删除 Key |
| 分布式锁 | 并发控制层 | 共享资源竞争 | 余额扣减、库存扣减 | Redisson | 死锁、锁释放问题 |
| 数据库唯一索引 | 持久层 | 数据完整性 | 数据唯一性约束 | MySQL/PostgreSQL | 性能开销、异常处理复杂 |

---

## 三、典型场景兜底分析

### 场景一：创建订单接口

#### 3.1 单击两次（用户快速点击）

**兜底层级**：前端 loading → 后端幂等控制

**流程说明**：

```
用户点击 ──→ 前端设置 formLoading=true ──→ 第 1 次请求发出
                    │
                    └──→ 第 2 次点击被按钮 disabled 拦截（第一层）
                    
如果第 1 次请求非常快，或前端逻辑有漏洞：
第 2 次请求到达后端 ──→ Redis SETNX 检查 key 是否已存在 ──→ 抛出"重复请求"异常（第二层）
```

#### 3.2 网络重试（HTTP 层/网关重试）

**兜底层级**：后端幂等控制 → 分布式锁 → 数据库唯一索引

**流程说明**：

```
正常请求 ──→ 后端处理中 ──→ 网络超时
                    │
                    └──→ 网关/客户端触发重试（同一个请求再次发出）

第 2 次请求到达后端：
1. 幂等 Redis Key 仍在有效期内（默认 1 秒）──→ 拦截（第一层）
2. 如果 Key 已过期（超过 timeout）──→ 分布式锁保护共享资源（第二层）
3. 分布式锁也失效 ──→ 数据库唯一索引冲突（第三层）
```

#### 3.3 浏览器刷新重放（POST-Redirect-GET 问题）

**兜底层级**：后端幂等控制（需要业务单号支持）

**问题描述**：用户提交表单后，在结果页刷新浏览器，浏览器可能重发 POST 请求。

**正确处理**：

1. 前端：使用 PRG（Post-Redirect-Get）模式，提交成功后跳转
2. 后端：使用业务唯一标识（如订单号）作为幂等 Key

```java
// 使用 ExpressionIdempotentKeyResolver，基于业务参数生成 Key
@Idempotent(
    keyResolver = ExpressionIdempotentKeyResolver.class,
    keyArg = "#reqVO.orderNo",  // 基于订单号生成 Key
    timeout = 60,  // 设置较长超时
    deleteKeyWhenException = false  // 成功后也保留 Key
)
@PostMapping("/order/create")
public CommonResult<Long> createOrder(@RequestBody OrderCreateReqVO reqVO) {
    // ...
}
```

#### 3.4 MQ 重复消费

**兜底层级**：分布式锁 → 状态机防重 → 数据库唯一索引

**流程说明**：

```
MQ 消息 A ──→ 消费者处理 ──→ 业务执行中
                    │
                    └──→ 消费者宕机 / 网络问题 ──→ MQ 重新投递

第 2 次消费：
1. 分布式锁（基于消息 ID 或业务 ID）──→ 等待或拒绝（第一层）
2. 状态机校验（订单是否已处理）──→ 幂等返回成功（第二层）
3. 数据库唯一索引（业务唯一约束）──→ 捕获 DuplicateKeyException（第三层）
```

**钱包充值回调示例**：`yudao-module-pay/src/main/java/cn/iocoder/yudao/module/pay/service/wallet/PayWalletRechargeServiceImpl.java:119-158`

```java
public void updateWalletRechargerPaid(Long id, Long payOrderId) {
    PayWalletRechargeDO recharge = walletRechargeMapper.selectById(id);
    
    // 状态机防重：已支付且支付单号相同，直接返回（MQ 重复消费场景）
    if (recharge.getPayStatus()) {
        if (ObjectUtil.equals(recharge.getPayOrderId(), payOrderId)) {
            log.warn("[updateWalletRechargerPaid][recharge({}) 已支付，且支付单号相同({})，直接返回]", recharge, payOrderId);
            return;  // 幂等返回，不报错
        }
        throw exception(WALLET_RECHARGE_UPDATE_PAID_PAY_ORDER_ID_ERROR);
    }
    
    // ... 后续处理
}
```

---

## 四、只靠前端 loading 会失败的 4 种情况

### 4.1 情况一：JS 执行环境被绕过

**失败场景**：
- 用户通过浏览器开发者工具，手动修改 DOM，移除按钮的 `disabled` 属性
- 用户直接调用接口（Postman、curl、脚本），完全绕过前端

**攻击示例**：
```javascript
// 控制台执行：
document.querySelector('button[type="primary"]').disabled = false;
// 或者直接调用 API：
fetch('/api/order/create', {
    method: 'POST',
    body: JSON.stringify({...})
});
```

**正确处理**：后端必须有幂等控制，不能信任前端。

---

### 4.2 情况二：网络层重试（网关/客户端重试）

**失败场景**：
- 请求已到达后端，正在处理中
- 网络超时，网关（Nginx/Zuul/Spring Cloud Gateway）配置了自动重试
- 同一请求被再次发送到后端

**问题分析**：前端 loading 只能防止用户重复操作，无法控制网络层的重试策略。

**时序图**：
```
客户端        网关          后端
   │            │            │
   │─POST──────>│            │
   │            │─POST──────>│
   │            │            │──处理中（耗时 3s）
   │            │            │
   │<─超时──────│            │  （网关超时 2s）
   │            │            │
   │            │─重试POST──>│  （同一请求再次发出）
   │            │            │──第 2 次处理开始
```

**正确处理**：后端 `@Idempotent` 注解 + 合理的 timeout 配置。

---

### 4.3 情况三：请求处理时间超过 loading 显示时间

**失败场景**：
- 用户点击按钮，前端显示 loading
- 请求发出，但后端处理非常慢（超过预期）
- 用户刷新页面，loading 状态丢失
- 用户再次点击，第 2 次请求发出

**问题代码**：
```javascript
// 问题：刷新页面后 formLoading 状态丢失
const formLoading = ref(false)

const submitForm = async () => {
    formLoading.value = true
    try {
        await slowApi()  // 假设这个 API 需要 10 秒
        // 用户在第 5 秒刷新了页面
    } finally {
        formLoading.value = false
    }
}
```

**正确处理**：
- 后端幂等控制（基于业务单号，设置较长 timeout）
- 关键操作使用持久化的幂等 Token 机制

---

### 4.4 情况四：并发请求（毫秒级）

**失败场景**：
- 用户使用脚本或自动化工具，在极短时间内（毫秒级）发出多个请求
- 第 1 个请求的 loading 状态还未传播到 UI 层
- 第 2、3 个请求已经发出

**问题分析**：JavaScript 是单线程的，但事件循环中：
```javascript
// 理论上的执行顺序
button.addEventListener('click', () => {
    formLoading.value = true  // 1. 设置状态
    await apiCall()           // 2. 发出请求（异步）
})

// 但在并发场景下：
// 第 1 次点击：事件 1 入队
// 第 2 次点击：事件 2 入队（在事件 1 的 await 之前）
// 事件循环执行：
//   - 事件 1 执行：设置 loading=true，发出请求
//   - 事件 2 执行：loading 可能还没更新到 DOM，或用户绕过
```

**正确处理**：后端 `Redis SETNX` 原子操作保证只有一个请求能获取锁。

---

## 五、后端正确实现的关键代码位置

### 5.1 幂等控制完整实现

| 文件 | 路径 | 作用 |
|------|------|------|
| `Idempotent.java` | `yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/idempotent/core/annotation/Idempotent.java` | 幂等注解定义 |
| `IdempotentAspect.java` | `yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/idempotent/core/aop/IdempotentAspect.java` | AOP 切面，核心逻辑 |
| `IdempotentRedisDAO.java` | `yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/idempotent/core/redis/IdempotentRedisDAO.java` | Redis 操作封装 |
| `DefaultIdempotentKeyResolver.java` | `yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/idempotent/core/keyresolver/impl/DefaultIdempotentKeyResolver.java` | 默认 Key 生成器 |

### 5.2 分布式锁完整实现

| 文件 | 路径 | 作用 |
|------|------|------|
| `Lock4jRedisKeyConstants.java` | `yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/lock4j/core/Lock4jRedisKeyConstants.java` | 锁 Key 常量定义 |
| `YudaoLock4jConfiguration.java` | `yudao-framework/yudao-spring-boot-starter-protection/src/main/java/cn/iocoder/yudao/framework/lock4j/config/YudaoLock4jConfiguration.java` | Lock4j 配置 |
| `PayWalletLockRedisDAO.java` | `yudao-module-pay/src/main/java/cn/iocoder/yudao/module/pay/dal/redis/wallet/PayWalletLockRedisDAO.java` | 钱包锁自定义实现 |
| `PayWalletServiceImpl.java` | `yudao-module-pay/src/main/java/cn/iocoder/yudao/module/pay/service/wallet/PayWalletServiceImpl.java` | 锁的实际使用（双重检查锁、余额扣减） |

### 5.3 数据库层面防重（状态机 + 乐观锁）

| 文件 | 路径 | 作用 |
|------|------|------|
| `PayWalletRechargeServiceImpl.java` | `yudao-module-pay/src/main/java/cn/iocoder/yudao/module/pay/service/wallet/PayWalletRechargeServiceImpl.java` | 支付回调状态机防重 |
| `PayOrderDO.java` | `yudao-module-pay/src/main/java/cn/iocoder/yudao/module/pay/dal/dataobject/order/PayOrderDO.java` | 订单数据模型（包含 merchantOrderId 业务唯一字段） |

---

## 六、最佳实践建议

### 6.1 创建订单接口推荐方案

```
用户操作 ──→ 前端 loading（第 1 层：防快速点击）
    │
    └──→ 后端 @Idempotent（第 2 层：防网络重试）
            │
            └──→ 分布式锁（第 3 层：防并发扣减）
                    │
                    └──→ 状态机校验 + 乐观锁（第 4 层：防 MQ 重复消费）
                            │
                            └──→ 数据库唯一索引（第 5 层：最终兜底）
```

### 6.2 不同场景的机制选择

| 业务场景 | 推荐机制组合 | 说明 |
|----------|--------------|------|
| 普通表单提交 | 前端 loading + @Idempotent | 简单有效 |
| 订单创建/支付 | 全部四层 | 涉及金额，必须严格防重 |
| 库存扣减 | 分布式锁 + 乐观锁 | 并发高，需要细粒度控制 |
| MQ 消息消费 | 状态机 + 唯一索引 | 消息可能重复投递 |

### 6.3 配置建议

```java
// 1. 短时间操作（< 1s）：默认配置
@Idempotent(timeout = 1, deleteKeyWhenException = true)

// 2. 长时间操作（如文件处理）：延长 timeout
@Idempotent(timeout = 60, deleteKeyWhenException = true)

// 3. 写入操作（不允许重复）：基于业务 Key，成功后也保留
@Idempotent(
    keyResolver = ExpressionIdempotentKeyResolver.class,
    keyArg = "#reqVO.orderNo",
    timeout = 3600,  // 1 小时
    deleteKeyWhenException = true,
    message = "订单已创建，请勿重复提交"
)

// 4. 余额扣减：必须加分布式锁
@Transactional(rollbackFor = Exception.class)
public PayWalletTransactionDO reduceWalletBalance(Long walletId, ...) {
    return lockRedisDAO.lock(walletId, UPDATE_TIMEOUT_MILLIS, () -> {
        // 业务逻辑
    });
}
```

---

## 七、总结

| 机制 | 能防住什么 | 防不住什么 | 在体系中的位置 |
|------|------------|------------|----------------|
| 前端 loading | 正常用户的快速点击 | 技术用户、网络重试、并发请求 | 第一道防线（用户体验层） |
| 后端幂等控制 | 短时间内的重复请求 | 长时间操作、Key 过期 | 第二道防线（业务控制层） |
| 分布式锁 | 共享资源的并发访问 | 锁释放异常、死锁 | 第三道防线（并发控制层） |
| 数据库唯一索引 | 数据完整性、最终一致性 | 性能开销、异常处理复杂 | 第四道防线（最终兜底） |

**核心原则**：**前端是锦上添花，后端才是真正的防线。** 任何重要的写操作，都不能只依赖前端控制，必须在后端有完整的防护机制。
